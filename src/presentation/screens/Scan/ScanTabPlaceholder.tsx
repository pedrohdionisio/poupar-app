/**
 * A tab `Scan` só reserva o slot central da tab bar — o toque é interceptado no
 * `AppTabNavigator` e abre o modal de scan. Esta tela nunca chega a ser exibida.
 */
export function ScanTabPlaceholder() {
  return null;
}
